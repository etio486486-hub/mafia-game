services:
  - type: web
    name: mafia-game
    runtime: node
    plan: free
    buildCommand: npm install && npm run prepare-assets
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
